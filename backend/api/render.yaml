services:
  - type: web
    name: solo-doctor-api
    runtime: node
    rootDir: backend/api
    buildCommand: npm install && npx prisma migrate deploy && npx prisma generate && npm run build
    startCommand: node dist/main.js
    envVars:
      - key: NODE_ENV
        value: production